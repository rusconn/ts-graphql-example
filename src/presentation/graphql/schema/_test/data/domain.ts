import { domain as refreshTokens } from "./domain/refresh-tokens.ts";
import { domain as todos } from "./domain/todos.ts";
import { domain as users } from "./domain/users.ts";

export const domain = {
  refreshTokens,
  todos,
  users,
};
