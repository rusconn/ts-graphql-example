import { mappers as todo } from "./mappers/todo.ts";
import { mappers as user } from "./mappers/user.ts";
import { mappers as userToken } from "./mappers/user-token.ts";

export const mappers = {
  todo,
  user,
  userToken,
};
