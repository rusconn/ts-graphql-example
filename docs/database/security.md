# Database Security

## RLS

DBのRLSは利用していない。セッションがステートフルになってしまうため、スケーラビリティに影響が出ると考えた。その代わり、各リポジトリへRLS相当の機能を付与し、アプリケーションレイヤーでRLSを実現している。

## credentialsの分離

パスワード等のセンシティブな情報は主要なテーブルに含めず別のテーブルに切り出すことで、不必要にデータを持ち回らないようにした。
