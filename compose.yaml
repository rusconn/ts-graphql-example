services:
  db:
    image: postgres:15.3-alpine3.18
    environment:
      - POSTGRES_USER
      - POSTGRES_PASSWORD
    volumes:
      - postgres:/var/lib/postgresql/data
    ports:
      - ${DB_PORT:?}:5432

volumes:
  postgres:
